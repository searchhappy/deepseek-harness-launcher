# DeepSeek Harness 双版本方案

## 📦 版本说明

现在项目提供两个版本供不同用户使用：

### 版本一：智能引导安装版（推荐给小白用户）

**文件名**: `DeepSeekHarness-Installer.exe`

**特点**:
- ✅ 自动检测 Node.js 是否安装
- ✅ 自动安装 DeepSeek Harness
- ✅ 一步步引导用户完成安装
- ✅ 提供手动下载按钮
- ✅ 友好的进度提示

**使用流程**:
1. 双击运行 `DeepSeekHarness-Installer.exe`
2. 自动检测环境
3. 如果缺少 Node.js，提供下载链接
4. 自动安装 DeepSeek Harness
5. 完成后点击"启动"按钮即可使用

**构建命令**:
```bash
npm run build:setup
```

---

### 版本二：标准便携版（当前版本）

**文件名**: `DeepSeekHarness-Portable.exe`

**特点**:
- ✅ 功能完整的控制台
- ✅ 启动、停止、清除端口等功能
- ✅ 实时日志显示
- ✅ 应用内窗口和浏览器模式

**要求**:
- 需要预先安装 Node.js

**构建命令**:
```bash
npm run build:win
```

---

## 🚀 开发和测试

### 测试智能安装版:
```bash
npm run start:setup
```

### 测试标准版:
```bash
npm start
```

---

## 💡 未来计划：真正的零依赖版

要创建完全零依赖的版本，需要：

1. **打包 Node.js 运行时**
   - 使用 pkg 或 nexe 将 Node.js 打包进应用
   - 预估大小：~100MB

2. **打包 DeepSeek Harness**
   - 将 `@deepseek-ai/dsh` 及其依赖打包
   - 预估大小：~50MB

3. **最终大小**
   - 预计总大小：150-200MB
   - 但用户体验最好，真正开箱即用

---

## 📋 当前文件结构

```
项目目录/
├── main.js                    # 标准版主进程
├── setup-main.js              # 安装版主进程
├── index.html                 # 标准版界面
├── setup.html                 # 安装向导界面
├── renderer.js                # 标准版前端
├── setup.js                   # 安装向导前端
├── package.json               # 标准版配置
├── package-setup.json         # 安装版配置
├── electron-builder-setup.json # 安装版构建配置
├── assets/
│   └── icon.ico              # DeepSeek 图标
└── dist/                      # 构建输出
    ├── DeepSeekHarness-Portable.exe      # 标准便携版
    └── DeepSeekHarness-Installer.exe     # 智能安装版
```

---

## 🎯 推荐使用场景

**智能安装版** - 适合：
- 完全不懂技术的小白用户
- 第一次接触 DeepSeek Harness
- 需要引导式安装体验

**标准便携版** - 适合：
- 已安装 Node.js 的用户
- 开发者和技术人员
- 需要完整控制功能

---

## 🔨 构建两个版本

```bash
# 构建标准便携版
npm run build:win

# 构建智能安装版
npm run build:setup
```

构建完成后，两个 exe 文件都在 `dist` 目录中。
