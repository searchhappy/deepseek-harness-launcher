# 构建问题说明

## 当前情况

由于网络限制，从 GitHub 下载 Electron 可能会失败。这是构建安装包时的常见问题。

## 📱 推荐方案：直接使用应用（无需构建）

**你不需要构建安装包也能完美使用应用！**

只需双击 **`快速启动.bat`** 或 **`启动应用.bat`**，应用就能正常运行，功能完全一样。

构建安装包主要用于：
- 分发给其他人使用
- 创建桌面快捷方式
- 看起来更"正式"

但对于个人使用，开发模式（直接运行）完全够用。

## 🔧 如果一定要构建

### 方法一：使用代理或 VPN

设置代理后再运行构建：
```bash
set HTTP_PROXY=http://你的代理地址:端口
set HTTPS_PROXY=http://你的代理地址:端口
npm run build:win
```

### 方法二：手动下载 Electron

1. 访问：https://npmmirror.com/mirrors/electron/28.3.3/
2. 下载：electron-v28.3.3-win32-x64.zip
3. 放到：`%LOCALAPPDATA%\electron\Cache\`
4. 重新运行构建

### 方法三：离线构建

如果有另一台网络环境好的电脑：
1. 在那台电脑上运行构建（会自动下载依赖）
2. 复制整个 `node_modules` 文件夹
3. 在当前电脑上构建

## 💡 最佳实践

**建议**：不要花时间纠结构建问题，直接使用开发模式运行应用：

```
✅ 快速启动.bat  ← 推荐使用这个
✅ 启动应用.bat
```

应用功能完全相同，启动速度也一样快！
