# 开箱即用安装程序方案

## 当前问题
用户安装后仍需要：
1. 手动安装 Node.js
2. 配置环境变量
3. 才能使用 DeepSeek Harness

## 🎯 三种开箱即用方案

### 方案 1：打包 Node.js 运行时（推荐 ⭐⭐⭐）

**原理**：将 Node.js 便携版打包进安装程序

**优点**：
- ✅ 用户安装后立即可用
- ✅ 不污染系统环境
- ✅ 不需要管理员权限
- ✅ 完全独立运行

**缺点**：
- ❌ 安装包增大约 30-50MB

**实现方式**：
1. 下载 Node.js 便携版
2. 打包到安装程序
3. 应用使用打包的 Node.js 运行

---

### 方案 2：在 NSIS 安装过程中自动安装 Node.js

**原理**：安装程序检测 Node.js，如果没有则自动下载安装

**优点**：
- ✅ 安装包较小
- ✅ Node.js 可供其他程序使用

**缺点**：
- ❌ 需要管理员权限
- ❌ 安装时间较长
- ❌ 需要网络连接
- ❌ 可能被杀毒软件拦截

---

### 方案 3：打包所有依赖（最彻底）

**原理**：预先安装 @deepseek-ai/dsh 并打包所有 node_modules

**优点**：
- ✅ 完全离线可用
- ✅ 启动最快

**缺点**：
- ❌ 安装包非常大（可能 200-300MB）

---

## 🌟 推荐：方案 1 + 预装依赖

结合方案 1 和方案 3 的优点：
1. 打包 Node.js 便携版（约 30MB）
2. 预装 @deepseek-ai/dsh（约 50MB）
3. 用户安装后直接运行，零配置

**最终效果**：
- 安装包大小：约 150-200MB
- 安装后立即可用
- 不需要网络
- 不需要管理员权限

---

## 你想选择哪个方案？

1. **方案 1**：打包 Node.js 运行时（安装包 ~150MB，推荐）
2. **方案 2**：自动安装 Node.js（安装包 ~80MB，需要网络）
3. **方案 1+3**：打包全部（安装包 ~200MB，最彻底）

请告诉我你的选择，我会立即实施！
