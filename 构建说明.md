# 构建说明

## 首次构建准备

由于 electron-builder 第一次运行时需要下载大量依赖（包括 Electron 二进制文件、NSIS 安装程序工具等），整个过程可能需要 10-20 分钟，具体时间取决于网络速度。

## 构建步骤

### 方法一：使用批处理文件（推荐）

1. 双击 `构建安装包.bat`
2. 等待构建完成（第一次会比较慢）
3. 完成后在 `dist` 目录找到安装程序

### 方法二：命令行构建

```bash
npm run build:win
```

## 构建输出

构建成功后，会在 `dist` 目录生成：
- `DeepSeek Harness Launcher Setup 1.0.0.exe` - 安装程序
- 其他辅助文件

## 常见问题

**Q: 构建很慢？**
A: 第一次构建需要下载依赖，后续构建会快很多。可以在命令行中运行查看详细进度。

**Q: 构建失败？**
A: 
1. 确保网络连接正常
2. 确保有足够的磁盘空间（至少 2GB）
3. 尝试删除 `node_modules` 文件夹后重新 `npm install`

**Q: 不想等待构建？**
A: 可以直接使用 `npm start` 运行开发版本，功能完全一样。

## 加速构建（可选）

如果网络较慢，可以设置国内镜像：

```bash
# 设置 Electron 镜像
set ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/

# 然后再运行构建
npm run build:win
```

## 分发应用

构建完成后，可以将 `dist` 目录中的安装程序分享给其他人使用。
