# DeepSeek Harness 便携版说明

## 📋 关于便携版

当前生成的 `DeepSeekHarness-Portable.exe` (67MB) 是一个 Electron 应用的便携版。

## ⚠️ 运行要求

**此应用仍需要以下环境：**

1. **Node.js** (必需)
   - 应用需要执行 `npx @deepseek-ai/dsh web` 命令
   - 下载：https://nodejs.org/

2. **网络连接** (首次运行)
   - 首次运行时 npx 会自动下载 `@deepseek-ai/dsh` 包
   - 之后可以离线使用

## ✅ 应用功能

- ✅ 启动服务按钮 - 启动 DeepSeek Harness
- ✅ 停止服务按钮 - 停止正在运行的服务
- ✅ 清除日志按钮 - 清空日志窗口
- ✅ 在应用内打开 - 在新窗口显示 DeepSeek Harness 界面
- ✅ 在浏览器中打开 - 在默认浏览器打开
- ✅ 实时日志显示 - 查看所有运行输出
- ✅ DeepSeek 官方图标

## 🎯 使用场景

**适合：**
- 已安装 Node.js 的用户
- 开发者和技术人员
- 需要便捷启动 DeepSeek Harness 的场景

**不适合：**
- 完全零技术背景的用户
- 没有 Node.js 环境的电脑

## 💡 如何分发

如果要分享给其他人：

1. **发送文件**：`DeepSeekHarness-Portable.exe`
2. **告知要求**：需要安装 Node.js
3. **使用说明**：
   - 双击运行应用
   - 点击"启动服务"
   - 首次运行会下载依赖（需要网络）
   - 之后可以离线使用

## 🔧 技术细节

- 基于 Electron 28.3.3
- 便携版无需安装，可以放在任何位置运行
- 关闭应用窗口会自动停止后台服务
- 配置和日志存储在用户目录

## 📝 替代方案

如果需要真正的"零依赖"版本，可以考虑：
1. 将 DeepSeek Harness 也打包进应用
2. 使用 pkg 或 nexe 将 Node.js 也打包
3. 创建一个安装包，自动安装所需环境

这些方案会使应用体积更大（可能超过 100MB），但用户体验更好。
